node_modules
dist
dist-ssr
.env
.env.local
*.local
.DS_Store
tsconfig.tsbuildinfo

# Estado local del CLI de Supabase -- .temp/ trae secretos reales del stack
# Docker (JWT, claves de Kong/DB), .branches/ es qué proyecto está linkeado
# en esta máquina. Ninguno de los dos debe viajar al repo.
supabase/.temp/
supabase/.branches/

.vercel
.env*
.claude/schema_apply.log
